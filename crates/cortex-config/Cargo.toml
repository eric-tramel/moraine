[package]
name = "cortex-config"
version = "0.1.1"
edition = "2021"
description = "Shared Cortex configuration schema and loaders"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
