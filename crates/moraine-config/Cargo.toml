[package]
name = "moraine-config"
version = "0.5.6"
edition = "2021"
description = "Shared Moraine configuration schema and loaders"

[dependencies]
anyhow = "1.0"
glob = "0.3"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
